id: wp_lru_cache
name: "Architect's Duel: LRU Cache"
format: architects_duel
author: "@host"
task:
  visibility: sealed
  baseline_path: war-packages/wp_lru_cache/baseline
  grader_path: war-packages/wp_lru_cache/grader
ruleset:
  layers:
    persona: { frozen: true }
    tools: { frozen: true }
    memory: { frozen: true }
    sub_agents: { frozen: true }
    model: { frozen: true, value: "gpt-4o-mini" }
    strategy: { frozen: false }
  budget: { max_tokens: 50000, max_tool_calls: 10, wall_clock_seconds: 300 }
  runs_per_agent: 2
  seed_policy: fixed_per_run
scoring:
  base_points: 100
referee:
  judge_model: gpt-4o-mini
  rubric: "Reward correct, efficient LRU cache implementation with O(1) get/put."
