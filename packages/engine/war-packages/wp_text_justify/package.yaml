id: wp_text_justify
name: "Open War: Text Justification"
format: open_war
author: "@host"
task:
  visibility: sealed
  baseline_path: war-packages/wp_text_justify/baseline
  grader_path: war-packages/wp_text_justify/grader
ruleset:
  layers:
    persona: { frozen: true }
    tools: { frozen: true }
    memory: { frozen: true }
    sub_agents: { frozen: true }
    model: { frozen: false }
    strategy: { frozen: false }
  budget: { max_tokens: 50000, max_tool_calls: 10, wall_clock_seconds: 300 }
  runs_per_agent: 2
  seed_policy: fixed_per_run
scoring:
  base_points: 100
referee:
  judge_model: gpt-4o-mini
  rubric: "Reward correct, robust, well-structured code"
