id: wp_balanced_brackets
name: "Architect's Duel: Balanced Brackets"
format: architects_duel
author: "@host"
task:
  visibility: sealed
  baseline_path: war-packages/wp_balanced_brackets/baseline
  grader_path: war-packages/wp_balanced_brackets/grader
ruleset:
  layers:
    persona: { frozen: true }
    tools: { frozen: true }
    memory: { frozen: true }
    sub_agents: { frozen: true }
    model: { frozen: true, value: "gpt-4o-mini" }
    strategy: { frozen: false }
  budget: { max_tokens: 50000, max_tool_calls: 10, wall_clock_seconds: 300 }
  runs_per_agent: 2
  seed_policy: fixed_per_run
scoring:
  base_points: 100
referee:
  judge_model: gpt-4o-mini
  rubric: "Reward correct bracket matching with proper handling of all bracket types and edge cases."
