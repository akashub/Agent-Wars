def median_lower(nums):
    """Return the median of a list of integers `nums`.

    IMPORTANT RULES (read carefully):
      - If `nums` is EMPTY, return None.
      - For an ODD-length list, the median is the exact middle value.
      - For an EVEN-length list, return the LOWER of the two middle values
        (NOT their average).
    The input may be unsorted and may contain negative numbers and duplicates.
    """
    raise NotImplementedError
