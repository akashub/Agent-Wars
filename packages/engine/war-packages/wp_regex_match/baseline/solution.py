def is_match(s, p):
    """Implement regular-expression matching where the pattern `p` must match the
    ENTIRE input string `s`.
      '.'  matches any single character.
      '*'  matches zero or more of the PRECEDING element.
    Return True if `p` matches all of `s`, else False. `s` has no special chars;
    `p` may contain lowercase letters, '.', and '*'.
    """
    raise NotImplementedError
